{{- if .isWSL }}
ghostty
{{- end }}
{{- if or (eq .chezmoi.os "darwin") .isWSL }}
containers
pipewire
systemd
wireplumber
mimeapps.list
{{- end }}
{{- if eq .chezmoi.os "darwin" }}
# macOS has no systemd user environment at all
environment.d
{{- else if .isWSL }}
# WSL keeps ssh-agent/editor env, but not Wayland/GUI env
environment.d/wayland-apps.conf
{{- end }}
